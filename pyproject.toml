[project]
name = "wine-chroot"
version = "0.1.0"
description = "Genera archivos .desktop para ejecutar aplicaciones Windows con Wine en entornos schroot de Debian"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
	"rich",
	"rich-argparse",
]
license = { file = "LICENSE" }
classifiers = [
	"License :: OSI Approved :: GNU General Public License v3 or later (GPLv3+)",
	"Programming Language :: Python :: 3",
	"Programming Language :: Python :: 3 :: Only",
	"Programming Language :: Python :: 3.10",
	"Programming Language :: Python :: 3.11",
	"Programming Language :: Python :: 3.12",
]
# Dependencias de Python gestionadas con uv.
# Dependencias del sistema:
#   Host: schroot, icoutils (wrestool, icotool)
#   Chroot: wine

[project.scripts]
wine-chroot = "python.make_wine_chroot_desktop:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["python"]

[dependency-groups]
dev = []
